import React from 'react';
import heroImage from '../img/hero-image.png';

const Hero = () => {
    return (
        <div>
            <img src={heroImage} alt="" />
        </div>
    )
}

export default Hero
